acceptance_tests:
  - '`uv run coc validate` exits 0 after the edit.'
  - The task schema's `output_targets` description addresses whether directory 
    or glob entries are acceptable vs. concrete file paths.
  - The task schema's `source_refs` field has a documented (and ideally 
    constrained) convention for unregistered sources — at minimum a short list 
    of accepted prefixes (e.g. `doi:`, `isbn:`, `url:`, `arxiv:`) alongside the 
    existing `src-NNNNNN--slug` form.
created_at: '2026-04-23T14:45:00Z'
id: tsk-20260423-000007
lease:
  attempts: 1
  last_heartbeat: '2026-04-23T17:46:07Z'
  leased_at: '2026-04-23T17:46:07Z'
  leased_by: Shiftor/20792
  max_attempts: 1
  ttl_minutes: 90
notes: "Cluster of 2 retro proposals against schemas/task.schema.json:\n\n- retro-01KPX8MD4GHQRN9QEYA2NMCEEM
  (severity minor) — document whether glob\n  patterns / directory paths are acceptable
  in `output_targets`, or whether\n  every entry must be a concrete file path. scout-systems
  emits N+1 files\n  whose exact paths are only knowable at execution time, which
  forced the\n  first Branch A run to use directory entries that validate today only\n\
  \  because the field has no pattern.\n- retro-01KPXBD1234567ABCDEFGHJKMN (severity
  minor) — document/constrain\n  `source_refs` prefix convention. scout-systems emits
  proposals whose\n  sources are not yet in registry/sources/; profile-system's SKILL
  implies\n  DOIs in manifests are acceptable, but the format is unspecified, so\n\
  \  agents will drift between `doi:10.x` / `https://doi.org/10.x` / bare\n  `10.x`
  without a canonical set.\n\nScope: limited to the schema + its description field.
  Do not bulk-rewrite\nexisting task manifests; any cleanup of manifests that depend
  on looser\nconventions can be a separate follow-up."
output_targets:
  - schemas/task.schema.json
priority: normal
skill: review-records
state: done
type: review-records
