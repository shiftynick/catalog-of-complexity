id: tsk-20260423-000008
type: review-records
skill: review-records
state: inbox
priority: normal
output_targets:
  - skills/scout-systems/SKILL.md
acceptance_tests:
  - '`uv run coc validate` exits 0 after the edit.'
  - skills/scout-systems/SKILL.md exposes canonical `acceptance_tests` strings (either in frontmatter or in a labelled block) that plan-backlog can copy verbatim into emitted manifests instead of reformulating them from `stop_conditions`.
  - '`uv run python scripts/lint_skills.py` passes on scout-systems.'
lease:
  ttl_minutes: 90
  max_attempts: 1
notes: |-
  Cluster of 1 retro proposal against skills/scout-systems/SKILL.md:

  - retro-01KPX8MD4GHQRN9QEYA2NMCEEM (severity minor) — plan-backlog
    instructs proposers to "pull the canonical shape from the target skill's
    SKILL.md. Do not fabricate acceptance tests", but scout-systems only
    exposes `stop_conditions`. That forced the first plan-backlog run to
    derive acceptance_tests text, which is mild fabrication by strict
    reading. Every future scout-systems proposal from plan-backlog will hit
    the same gap until the skill publishes canonical test strings.

  Scope: add a canonical acceptance_tests block (or equivalent structured
  reference) to the scout-systems skill. The same pattern may need to
  propagate to other skills surfaced by plan-backlog (profile-system,
  define-metrics, extract-observations, review-records) — file follow-up
  tasks if the review uncovers the same gap there, do not bulk-edit in this
  task.
created_at: '2026-04-23T14:45:00Z'
