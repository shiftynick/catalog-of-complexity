id: tsk-20260425-000016
type: review-records
skill: review-records
state: inbox
priority: high
output_targets:
  - registry/systems/sys-000001--amazon-rainforest/system.yaml
  - registry/systems/sys-000001--amazon-rainforest/notes.md
acceptance_tests:
  - 'sys-000001 (Amazon rainforest) has `status: deprecated` set on its
    `system.yaml`, `updated_at` bumped, and a "Deprecation" section
    appended to its `notes.md` explaining the type-level reframing per
    AGENTS.md "What counts as a system worth cataloging" — the Amazon
    is an *instance* of a forest-biome / ecosystem type, not a
    type-level archetype.'
  - 'The notes.md deprecation section names the replacement type slug(s)
    that supersede this entry — `ecosystem` (in
    config/priority-systems.yaml; class addition pending) and/or
    `forest-biome` (already in taxonomy/source/system-classes.yaml).'
  - '`uv run coc validate` exits 0 after the edit.'
lease:
  ttl_minutes: 90
  max_attempts: 1
created_at: '2026-04-25T20:30:00Z'
notes: |
  Bootstrap-era instance entry retired under the new inclusion criterion
  (AGENTS.md "What counts as a system worth cataloging"). The Amazon
  rainforest is a specific named biome, not a type-level archetype.

  Reviewer steps:
    1. Set `status: deprecated` and bump `updated_at` on
       registry/systems/sys-000001--amazon-rainforest/system.yaml.
    2. Append a "Deprecation" section to notes.md citing this task id,
       naming the replacement type slug(s) (`ecosystem`, `forest-biome`),
       and noting that the rich existing prose can be reused as a
       canonical example under the eventual type-level entry.
    3. Do NOT delete files — deprecated records remain on disk for
       audit/history.

  No source acquisition needed. The single observation against this
  system (obs-11111111, value-less stub) is unaffected by deprecation
  but downstream `extract-observations` will exclude deprecated
  systems from its scope.

  Companion deprecations land in the same review-records batch:
    tsk-20260425-000017  sys-000002 gut microbiome    → microbiome
    tsk-20260425-000018  sys-000004 C. elegans        → multicellular-organism
    tsk-20260425-000019  sys-000005 neocortex         → nervous-system
    tsk-20260425-000020  sys-000006 rhizosphere micro → microbiome
  sys-000003 (vertebrate adaptive immune system) is the only bootstrap
  entry kept as type-ish — borderline, may be re-evaluated later.
