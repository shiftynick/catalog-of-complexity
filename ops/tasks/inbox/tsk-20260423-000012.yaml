id: tsk-20260423-000012
type: review-records
skill: review-records
state: inbox
priority: normal
output_targets:
  - src/coc/queue.py
acceptance_tests:
  - '`uv run coc validate` exits 0 after the edit.'
  - '`complete_task()` in src/coc/queue.py validates `outputs_json` and any other inputs required for the `task.complete` event BEFORE renaming the task file out of `leased/`. A malformed `--outputs` argument must leave the task in `leased/` and must not produce a state where the file has moved to `done/` without a corresponding `task.complete` event.'
  - 'Either an existing or a new test exercises the failure path (malformed outputs json) and asserts the task file remains in `leased/` with no orphan `task.complete` event appended.'
lease:
  ttl_minutes: 90
  max_attempts: 1
notes: |-
  Cluster of 1 moderate retro proposal against src/coc/queue.py:

  - retro-01KPXDCKSJDNZQQMETHJD02J8D (severity moderate) — `uv run coc
    complete` moved the task from `leased/` to `done/` before parsing
    `--outputs`, so a malformed CLI argument left the queue state mutated
    without an automatic `task.complete` event. Ordering fix: parse and
    validate `outputs_json` before mutating task state; only rename into
    the terminal directory after event-append preconditions succeed.

  Scope: atomicity fix in `complete_task()` (and, if needed, the CLI
  wrapper) so the rename and event append behave as a single unit from
  the caller's perspective. Add a regression test on the malformed-
  outputs path. Do NOT widen scope to other queue transitions in this
  task — if a reviewer finds the same pattern in `lease_task` /
  `block_task` / `fail_task`, emit follow-up review-records tasks rather
  than bundling.
created_at: '2026-04-23T17:51:17Z'
