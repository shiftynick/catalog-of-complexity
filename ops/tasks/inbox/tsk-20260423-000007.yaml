id: tsk-20260423-000007
type: review-records
skill: review-records
state: inbox
priority: normal
output_targets:
  - schemas/task.schema.json
acceptance_tests:
  - '`uv run coc validate` exits 0 after the edit.'
  - 'The task schema''s `output_targets` description addresses whether directory or glob entries are acceptable vs. concrete file paths.'
  - 'The task schema''s `source_refs` field has a documented (and ideally constrained) convention for unregistered sources — at minimum a short list of accepted prefixes (e.g. `doi:`, `isbn:`, `url:`, `arxiv:`) alongside the existing `src-NNNNNN--slug` form.'
lease:
  ttl_minutes: 90
  max_attempts: 1
notes: |-
  Cluster of 2 retro proposals against schemas/task.schema.json:

  - retro-01KPX8MD4GHQRN9QEYA2NMCEEM (severity minor) — document whether glob
    patterns / directory paths are acceptable in `output_targets`, or whether
    every entry must be a concrete file path. scout-systems emits N+1 files
    whose exact paths are only knowable at execution time, which forced the
    first Branch A run to use directory entries that validate today only
    because the field has no pattern.
  - retro-01KPXBD1234567ABCDEFGHJKMN (severity minor) — document/constrain
    `source_refs` prefix convention. scout-systems emits proposals whose
    sources are not yet in registry/sources/; profile-system's SKILL implies
    DOIs in manifests are acceptable, but the format is unspecified, so
    agents will drift between `doi:10.x` / `https://doi.org/10.x` / bare
    `10.x` without a canonical set.

  Scope: limited to the schema + its description field. Do not bulk-rewrite
  existing task manifests; any cleanup of manifests that depend on looser
  conventions can be a separate follow-up.
created_at: '2026-04-23T14:45:00Z'
