id: tsk-20260423-000011
type: review-records
skill: review-records
state: inbox
priority: normal
output_targets:
  - skills/profile-system/SKILL.md
acceptance_tests:
  - '`uv run coc validate` exits 0 after the edit.'
  - 'skills/profile-system/SKILL.md no longer instructs agents to produce records with `status: active`; it names the schema-valid lifecycle value (`status: profiled`) and matches what existing system records already use.'
  - 'skills/profile-system/SKILL.md Procedure step 3 reconciles its `scales` description with `schemas/system.schema.json` — either the `organizational` axis is dropped from the skill prose, or a follow-up proposal/task is emitted to extend the schema. The skill and schema no longer disagree on what sub-fields `scales` permits.'
lease:
  ttl_minutes: 90
  max_attempts: 1
notes: |-
  Cluster of 7 moderate retro proposals against skills/profile-system/SKILL.md,
  drawn from 5 consecutive profile-system retros. Two recurring divergences
  between the skill text and `schemas/system.schema.json`:

  Theme A — `status: active` vs `status: profiled`:
  - retro-01KPXDCKSJDNZQQMETHJD02J8D (severity moderate) — skill procedure
    and stop_conditions say `status: active`, but the schema enum is
    `["candidate","profiling","profiled","deprecated"]`. Agent had to make
    the call on-run and picked `profiled` to match existing records.
  - retro-01KPXE59VNY48PC0CDNRR475B2 (severity moderate) — second
    profile-system run confirming the recurrence; same judgment call made
    against the same mismatch.
  - retro-01KPXNK8WS4Y4PHXF5TD6MHEZ9 (severity moderate) — third run;
    acceptance_tests on tsk-20260423-000005 again said "status active"
    which no existing system record has ever satisfied.
  - retro-01KPXNW5Z4F6EZ5TX00AK6KDSC (severity moderate) — fourth run;
    tsk-20260423-000006's acceptance_tests again restated "status active".
  - retro-7WT0NHAFNFMH7X96M2WW0HF3PM (severity moderate) — fifth run;
    same divergence described independently.

  Theme B — `scales.organizational` does not exist in the schema:
  - retro-01KPXNK8WS4Y4PHXF5TD6MHEZ9 (severity moderate) — skill step 3
    lists `spatial, temporal, organizational`, but the schema's `scales`
    object sets `additionalProperties: false` and only declares `spatial`
    and `temporal`. Three consecutive profile runs (sys-000003..000005)
    silently dropped the organizational axis.
  - retro-01KPXNW5Z4F6EZ5TX00AK6KDSC (severity moderate) — fourth
    consecutive run silently dropping organizational; flagged as signal
    rather than noise.

  Scope: documentation edit to skills/profile-system/SKILL.md (plus, if
  the reviewer chooses the "codify profiled" path, an aligned update to
  the profile-system task manifest template referenced from the skill).
  Do NOT modify existing system records (they already use `profiled`).
  Do NOT modify `schemas/system.schema.json` as part of this task — if
  the reviewer prefers to extend the schema (add `active` to the status
  enum, or add `organizational` to `scales`), emit a follow-up task
  targeting the schema instead of making the change here; those are
  sensitive-path edits that belong in their own review-records cluster.
created_at: '2026-04-23T17:51:17Z'
